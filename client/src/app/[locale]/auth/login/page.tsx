import { AuthForm } from '@/components';

const LoginPage = () => {
  return (
    <div className="auth-page" style={{ height: '100%' }}>
      <AuthForm />
    </div>
  );
};

export default LoginPage;
